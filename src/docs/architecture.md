# Architecture

> Complete this in story 18. Describe the three stages (CLI, HTTP API,
> React) and how a request flows through validate -> store original ->
> generate variants -> store variants -> manifest.
